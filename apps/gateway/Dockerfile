# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @twitch-microservice-nx/gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @twitch-microservice-nx/gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @twitch-microservice-nx/gateway && \
          adduser --system -G @twitch-microservice-nx/gateway @twitch-microservice-nx/gateway

COPY apps/gateway/dist @twitch-microservice-nx/gateway/
RUN chown -R @twitch-microservice-nx/gateway:@twitch-microservice-nx/gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @twitch-microservice-nx/gateway --omit=dev -f install

CMD [ "node", "@twitch-microservice-nx/gateway" ]
